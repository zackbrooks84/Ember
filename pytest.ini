[pytest] 
testpaths = tests 
addopts = -ra
markers =
    empirical: tests that rely on empirical snapshots/“golden” values
    low_drift: describe tests with low drift expectations
    high_drift: describe tests with high drift expectations
